prefix_rule(
  pattern = [["rm", "mv", "rmdir"]],
  decision = "prompt",
  justification = "削除や移動は不可逆になりやすいため都度確認する。",
  match = [
    "rm file.txt",
    "rm -rf tmp",
    "mv old.txt new.txt",
    "rmdir empty_dir",
  ],
  not_match = [
    "rg -n \"tmp\" README.md",
  ],
)
